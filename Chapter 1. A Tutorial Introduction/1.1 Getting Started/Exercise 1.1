#include <stdio.h>
//no #include <stdio.h> results in "Call to function 'printf' with no prototype in function main"
//no main() results in "Declaration terminated incorrectly"
//no printf results in "Code has no effect in function main"
//no brackets, parentheses, semi-colon, etc results in incomplete function

main()
{
    printf("Hello World\n");
}
